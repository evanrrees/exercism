#!/usr/bin/env bash

#exercise=$(basename $(dirname "$1"))
#solution_file="${1}/!(test).sh"
#test_file="${1}/@(test).sh"
#read_me="${1}/README.md"

#tmux new-session -d 'nano "$solution_file"'
#tmux split-window -h 'less -S "$read_me"'
#tmux split-window -v 'less -S "$test_file"'
#tmux -2 attach-session -d
